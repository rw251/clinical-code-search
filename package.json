{
  "name": "clin-code-db",
  "version": "0.0.1",
  "description": "DB schema and import process for clinical code lists",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Richard Williams",
  "license": "MIT",
  "devDependencies": {
    "sqlite3": "^3.0.8",
    "stream": "0.0.2"
  }
}
